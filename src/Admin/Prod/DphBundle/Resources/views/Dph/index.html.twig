{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Dph list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Visible</th>
                <th>Multiple</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('dph_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.visible }}</td>
                <td>{{ entity.multiple }}</td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('dph_show', { 'id': entity.id }) }}">show</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% endblock %}
